# WeMONEY Android — Capacitor Plan

Build Android from the existing web application without rewriting the web application.

- Keep `src/` as the primary web source.
- Keep Android-specific files in `android/`.
- Keep web deployment independently buildable.
- Use a dedicated Android branch for integration work.
- Build a debug APK first, then test, then build release APK.
